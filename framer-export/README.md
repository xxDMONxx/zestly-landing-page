# Framer Export (Zestly Data Harvest)

This folder contains notes to help recreate components in Framer.

- Use the colors and spacing from the root README and Tailwind config.
- Import assets from `/public/`.
- For simplified components, replicate the layout and animations with standard Framer behaviors (no custom code required).
